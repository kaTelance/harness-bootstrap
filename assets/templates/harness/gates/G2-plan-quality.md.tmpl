# 实施计划质量闸门（G2）

> 约束 AI 在产出 plan / tasks 时的行为。确保计划 Step 级可执行、可验证。

---

## 核心原则

计划必须拆解到「单一动作」粒度（2-5 分钟一步），每步有明确的文件路径、操作内容、验证标准。禁止用「实现功能 X」这种粗粒度作为一步。

---

## 触发条件

- 产出 plan 或 tasks 文档（Step 级拆解）
- 把需求转化为可执行任务序列

---

## 必做流程

1. **Step 级拆解**：每个 Step 是一个原子动作（写测试 / 跑测试验证失败 / 写实现 / 跑测试验证通过 / commit）。
2. **文件路径明确**：每步标注 Create/Modify/Test 的精确路径与行号。
3. **完整内容**：代码步骤给出实际代码，非「类似 Task N」「适当处理」。
4. **验证标准**：每步有可执行的验证命令 + 预期输出。
5. **依赖排序**：Step 间依赖清晰，先决条件在前。
6. **验收标准**：计划末尾定义「完成」的可验证标准。

---

## 计划质量检查清单

- [ ] 每个 Step 是否单一动作（2-5 分钟）？
- [ ] 每步是否标注精确文件路径？
- [ ] 代码步骤是否给出实际代码（无 TBD/TODO/占位符）？
- [ ] 每步是否有验证方式 + 预期输出？
- [ ] 是否定义了验收标准？
- [ ] 是否遵循 DRY / YAGNI / TDD / 频繁 commit？

---

## 禁止事项

- 禁止「实现功能 X」粗粒度 Step。
- 禁止步骤无验证标准。
- 禁止计划含 TBD/TODO/「适当处理」/「类似 Task N」。
- 禁止跳过验收标准定义。
- 禁止描述「做什么」而不给「怎么做」（代码步骤必须有代码）。
