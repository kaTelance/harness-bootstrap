# UI 原型确认闸门（条件）

> 【条件启用】有 UI 的项目（Web / 桌面 / 移动）。新页面 / 新卡片 / 新交互前强制原型确认。

---

## 核心原则

无原型不 UI。新增页面 / 卡片 / 交互前，必须先有视觉原型（或设计参考），确认后再编码。色值 / 间距 / 圆角 / 阴影必须来自 design token。

---

## 触发条件

- 新增页面 / 视图 / 屏幕
- 新增卡片 / 组件（画布上 / 列表中 / 弹层中）
- 新增交互逻辑（拖拽 / 动画 / 手势 / 复杂状态流转）

---

## 必做流程

1. 查原型：`docs/prototypes/` 或当前 change 的 `design.md` 是否有原型。
2. 有确认原型 → 严格按原型编码。
3. 无原型 → 先生成原型（基于设计规范），交付确认后再编码。
4. 色值 / 间距 / 圆角 / 阴影引用 design token，不硬编码。
5. 列出「原型色值 / 尺寸 → design token」对照表后再编码。

---

## 检查清单

- [ ] 有确认的原型吗？
- [ ] 实现与原型一致吗？
- [ ] 色值 / 间距 / 圆角引用 token 吗（无硬编码）？
- [ ] 有原型 → token 对照表吗？

---

## 禁止事项

- 禁止无原型直接编码 UI。
- 禁止硬编码色值 / 间距。
- 禁止实现偏离原型（除非有设计变更记录）。
