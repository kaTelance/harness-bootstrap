# 实施执行质量闸门（G7）

> 约束 AI 在 apply（编码实施）阶段的行为。确保 TDD 纪律、无占位符、代码完整、频繁 commit。

---

## 核心原则

apply 阶段必须严格遵循已审计划 + TDD 纪律。每步有验证（测试通过 / 编译通过 / 类型检查通过），代码完整无占位符。

---

## 触发条件

- 进入 apply（编码实施）阶段
- 执行计划中的 Task

---

## 必做流程

1. 按已审计划执行，不偏离。
2. TDD：先写失败测试 → 实现 → 测试通过。
3. 每步验证：测试 / 编译 / 类型检查。
4. 代码完整：无 TODO / TBD / 占位符 / 「适当处理」。
5. 频繁 commit：每完成一个可验证单元就 commit（WIP）。
6. 偏离计划 → 回 G2 修订计划，不在 apply 中即兴发挥。

---

## 检查清单

- [ ] 是否按已审计划执行？
- [ ] 是否 TDD（先写失败测试，再实现）？
- [ ] 每步都验证了吗（测试 / 编译 / 类型）？
- [ ] 代码无 TODO / TBD / 占位符？
- [ ] 频繁 commit 了吗？
- [ ] 编译 / 类型 / 测试都通过？
- [ ] 改动是否只 stage 当前任务相关文件？

---

## 禁止事项

- 禁止跳过测试直接实现。
- 禁止代码留 TODO / TBD / 占位符。
- 禁止不验证就声明完成。
- 禁止偏离计划不回 G2。
- 禁止囤积大量改动不 commit。
- 禁止混入无关改动到当前 commit。
