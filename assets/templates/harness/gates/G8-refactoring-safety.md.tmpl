# 重构安全（G8）

> 约束 AI 在重构时的行为。重构涉及状态迁移、事件链路、盒模型 / 布局时强制等价性验证。

---

## 核心原则

重构必须保持行为不变。涉及状态迁移、事件链路、盒模型 / 布局的重构，必须有等价性验证（测试覆盖 + 行为对比）。

---

## 触发条件

- 重构涉及状态迁移（state shape 变化）
- 重构涉及事件链路（事件触发顺序 / 监听变化）
- 重构涉及盒模型 / 布局（CSS / 尺寸 / 定位变化）

---

## 必做流程

1. 确认重构目标（保持行为不变，改善结构）。
2. 确认测试覆盖：重构前有足够测试证明当前行为；不足则先补测试。
3. 小步重构：每步可独立验证。
4. 状态迁移：提供迁移逻辑 + 旧数据兼容。
5. 事件链路：验证触发顺序不变 + 监听者不遗漏 / 不重复。
6. 盒模型 / 布局：视觉回归对比（关键视图前后一致）。
7. 每步跑测试 + 回归验证。

---

## 检查清单

- [ ] 重构前测试覆盖足够吗？
- [ ] 状态迁移有兼容逻辑吗？
- [ ] 事件链路触发顺序不变吗？监听者无遗漏？
- [ ] 盒模型有视觉回归对比吗？
- [ ] 每步都验证了吗？

---

## 禁止事项

- 禁止无测试覆盖就重构。
- 禁止重构改变行为（除非是 bug fix，且走 G9 BugFix 协议）。
- 禁止大跨度重构不验证。
- 禁止状态迁移破坏旧数据。
