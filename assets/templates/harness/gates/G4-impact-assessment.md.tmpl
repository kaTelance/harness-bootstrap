# 影响评估闸门（G4）

> 约束 AI 在修改已有代码前的行为。强制评估变更影响范围，防止「改动很小」思维导致回归。

---

## 核心原则

修改已有代码前，必须先评估影响范围：触及哪些符号、调用方、被调用方、状态、数据流。禁止凭「改动很小」直接动手。

---

## 触发条件

- 计划阶段已完成（G3 通过），进入 apply 前评估
- 修改任何已有文件中的代码（非新增）

---

## 必做流程

1. 标注修改的符号（函数 / 类 / 变量 / 接口 / 类型）。
2. 列出调用方（谁依赖它）—— grep 或 CodeGraph 查找所有引用点。
3. 列出被调用方（它依赖什么）。
4. 评估状态 / 数据流影响（是否改变数据形状、事件顺序、副作用）。
5. 评估跨模块 / 跨层影响。
6. 标注高风险点（公共 API、热路径、并发点）。

---

## 检查清单

- [ ] 修改的符号明确吗？
- [ ] 所有调用方都列出吗？
- [ ] 有没有遗漏的间接依赖（反射 / 动态分发 / 配置驱动）？
- [ ] 影响范围是否在可控范围内？
- [ ] 高风险点是否标注并准备验证策略？

---

## 禁止事项

- 禁止「改动很小」不评估。
- 禁止只改目标函数不评估调用方。
- 禁止修改公共接口 / 类型签名而不评估所有消费者。
- 禁止跳过评估直接编码。
