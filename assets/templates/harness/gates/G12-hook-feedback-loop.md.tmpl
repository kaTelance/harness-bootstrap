# Hook 反馈闭环（G12）

> 闸门不通过 / invariant 破坏 / 闸门跳过时，强制闭环处理。防止忽视 hook 反馈。

---

## 核心原则

当 hook 触发反馈（提醒 / 阻断 / invariant 破坏告警）时，必须正面响应并闭环：理解反馈 → 修正 → 验证闭环。禁止忽视或绕过。

---

## 触发条件

- hook 输出反馈（提醒 / 阻断 / invariant 告警）
- 闸门未通过
- 闸门被跳过

---

## 必做流程

1. **读取反馈**：理解 hook 在说什么（哪个闸门 / 哪个 invariant）。
2. **定位问题**：反馈指向的具体行为是什么。
3. **修正**：按闸门 / invariant 要求修正。
4. **验证闭环**：重新触发场景，确认反馈不再出现。
5. **记录**：反复触发的反馈 → 沉淀到 `.harness/ANTI_PATTERNS.md`。

---

## 检查清单

- [ ] hook 反馈读懂了吗？
- [ ] 修正对应的问题了吗？
- [ ] 验证反馈不再出现了吗？
- [ ] 反复触发的反馈沉淀到 ANTI_PATTERNS 了吗？

---

## 禁止事项

- 禁止忽视 hook 反馈。
- 禁止绕过 hook（如用 Bash 绕过 Write 闸门来规避审查）。
- 禁止修正了不验证闭环。
- 禁止反复触发同一反馈不沉淀。
