# 平台规则闸门（条件）

> 【条件启用】跨平台项目（如 macOS + Windows、Linux + Windows）。处理平台差异时强制考虑兼容性。

---

## 核心原则

跨平台行为必须显式处理平台差异（路径分隔符 / 换行符 / 文件系统 / 键位 / 系统调用）。禁止用单一平台假设写跨平台代码。

---

## 触发条件

- 文件路径处理
- 文件系统操作（权限 / 符号链接 / 大小写敏感 / 路径长度）
- 平台特有 API / 行为
- 快捷键 / 输入处理

---

## 必做流程

1. 识别平台差异点。
2. 使用跨平台 API / 抽象层（如 `path.join` 而非字符串拼接）。
3. 平台分支逻辑显式 + 注释说明为什么分支。
4. 每个支持平台都验证（至少编译 + 关键路径测试）。

---

## 检查清单

- [ ] 平台差异点识别了吗？
- [ ] 用跨平台 API / 抽象层了吗？
- [ ] 平台分支有注释说明吗？
- [ ] 每个平台都验证了吗？

---

## 禁止事项

- 禁止用单一平台假设（如硬编码 `\` 或 `/`、假设大小写敏感、假设行结束符）。
- 禁止只在一个平台验证就声明跨平台支持。
