# 设计规范同步闸门（条件）

> 【条件启用】有设计系统的项目。修改 design token / 设计规范 / 原型时强制同步所有消费者。

---

## 核心原则

design token 是单一事实源。修改 token 必须同步所有消费者（CSS / 代码常量 / 文档）。样式实现必须与规范一致。

---

## 触发条件

- 修改 design token 定义
- 修改设计规范文档
- 修改原型
- 新增 / 修改样式

---

## 必做流程

1. token 改动 → 同步所有引用点（CSS custom property / 代码常量 / 文档示例）。
2. 样式实现 → 对照规范验证一致。
3. 新增 token → 文档 + 类型定义 + CSS 三处同步。
4. 同步检查：实现与规范一致（本闸门核心）。

---

## 检查清单

- [ ] token 改动所有消费者同步了吗？
- [ ] 样式实现与规范一致吗？
- [ ] 新增 token 三处（文档 / 类型 / CSS）同步吗？
- [ ] 是否引入了规范外的新样式值？

---

## 禁止事项

- 禁止改 token 不同步消费者。
- 禁止样式实现偏离规范。
- 禁止在代码中引入规范外的硬编码样式值。
