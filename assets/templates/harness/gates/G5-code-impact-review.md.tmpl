# 代码影响深度审查（G5）

> 约束 AI 在修改已有函数前的行为。强制「三次阅读」（目标函数、调用方、被调用函数）并产出行为依赖表。即 Read Before Write 铁律。

---

## 核心原则

无阅读不修改。修改任何已有函数前，必须先完成三次阅读，理解其行为、被谁依赖、依赖什么，才能安全修改。

---

## 触发条件

- 修改已有文件中的逻辑代码（非新增、非纯格式化）
- 修改公共函数 / 接口 / 类型签名

---

## 必做流程

1. **读目标函数**：理解当前行为、签名、边界条件、副作用、错误处理。
2. **读调用方**：grep 所有调用点，理解每个调用方对该函数的预期（输入假设、输出依赖、异常处理）。
3. **读被调用函数**：理解它依赖的下游函数契约（参数、返回、异常）。
4. **产出行为依赖表**：
   - 当前行为：函数做什么、契约是什么
   - 调用方：谁依赖它、依赖什么行为
   - 被调用：它依赖什么
   - 修改影响：改后哪些调用方的预期会受影响

---

## 检查清单

- [ ] 三次阅读都完成了吗？
- [ ] 行为依赖表产出了吗？
- [ ] 所有调用方都覆盖了吗？
- [ ] 修改是否破坏现有契约？
- [ ] 边界条件 / 错误路径都考虑了吗？

---

## 禁止事项

- 禁止不读就改。
- 禁止只读目标函数不读调用方。
- 禁止修改后不验证调用方行为。
- 禁止假设「调用方不会受影响」而不去验证。
