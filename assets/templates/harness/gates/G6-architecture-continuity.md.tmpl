# 架构延续（G6）

> 约束 AI 在任何代码或文档变更时保持架构一致性。防止引入与现有架构冲突的模式。

---

## 核心原则

新代码必须延续既有架构模式（目录结构、分层、命名、依赖方向、数据流）。禁止引入与现有约定冲突的新模式，除非经过明确的设计决策并记录。

---

## 触发条件

- 任何代码或文档变更
- 新增文件、模块、组件

---

## 必做流程

1. 识别当前架构模式（查 project-context.md §结构 + 既有代码）。
2. 确认新代码遵循既有模式（目录位置、命名、分层、依赖方向）。
3. 若需偏离既有模式，先在设计文档 / spec 中说明理由 + 获得确认。
4. 保持依赖方向正确（不引入循环依赖、不跨层、不反向依赖）。
5. 新增的约定若会成为模式，回写 project-context.md。

---

## 检查清单

- [ ] 新文件放在正确的目录？
- [ ] 命名遵循既有约定？
- [ ] 分层正确（不跨层调用）？
- [ ] 依赖方向正确（无循环、无反向）？
- [ ] 如偏离模式，有设计决策记录并获得确认？

---

## 禁止事项

- 禁止引入与既有架构冲突的模式。
- 禁止跨层依赖（如 UI 直接访问数据库层）。
- 禁止循环依赖。
- 禁止无记录地偏离架构约定。
