# 重复修复升级（G10）

> 同一 bug 第 2 次修复失败后触发升级。强制停止「再试一次」，转入系统化排查。

---

## 核心原则

同一 bug 修复失败 2 次后，禁止继续用相同思路「再试一次」。必须升级到系统化排查（扩大假设空间 / 寻求外部视角 / 质疑前提）。

---

## 触发条件

- 同一 bug 第 2 次修复失败

---

## 必做流程

1. **停止重试**：禁止用相同思路第 3 次。
2. **复盘前 2 次**：为什么失败？共同假设是什么？
3. **质疑前提**：之前的根因假设是否错了？复现路径是否完整？是否只覆盖了部分触发条件？
4. **扩大假设空间**：考虑之前排除的可能性（并发 / 时序 / 环境 / 数据相关 / 第三方行为）。
5. **寻求外部视角**：调用领域专家子代理 / 列给用户审查。
6. **竞争性修复**：并行启动 ≥ 2 个 debugger 子代理走不同方案（若工具支持）。

---

## 检查清单

- [ ] 前 2 次失败的共同假设识别了吗？
- [ ] 前提被质疑了吗？
- [ ] 假设空间扩大了吗（并发 / 时序 / 环境）？
- [ ] 是否引入了外部视角？

---

## 禁止事项

- 禁止第 3 次用相同思路。
- 禁止不质疑前提就继续。
- 禁止闭门重试（必须扩大视角）。
