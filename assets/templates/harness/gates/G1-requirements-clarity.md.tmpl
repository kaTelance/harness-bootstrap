# 需求清晰度闸门（G1）

> 约束 AI 在 propose/apply、计划和开发前的行为。防止需求/验收标准不明确时自由发挥。

---

## 核心原则

任何影响范围、行为、数据模型、用户体验、性能、安全、验收标准或文档同步的模糊点，都必须先澄清，再计划或开发。

AI 可建议、列选项、推荐方案，但不能把未确认建议当成事实写入实现。

---

## 触发条件

以下任一出现时进入澄清模式：

- 当前功能点在 `.harness/openspec/specs/` 和 `docs/` 中找不到明确依据
- 用户描述含「差不多/应该/类似/优化一下/完善一下」等模糊目标
- 需求涉及新能力、新数据模型、新接口、新流程、新性能预算或跨平台行为
- 现有文档互相矛盾
- AI 推断的实现路径超出用户明确表达的范围
- 实现中发现需求新增、范围变化或原设计不成立

---

## 必做流程

1. **查依据**（按 project-context.md §7 信息优先级）
   - 优先读 `.harness/openspec/specs/**/spec.md`（Tier 2）
   - 必要时读 `docs/*.md`（Tier 3，先读 `docs/DOCS_INDEX.md` 定位）
2. **列不确定点**：明确写出「已确认事实」和「待确认问题」，每个待确认问题说明影响什么决策。
3. **领域专家追问**：调用 `.claude/agents/` 下相关领域专家（按项目画像），逐项澄清。
4. **先问再做**：优先用 brainstorming；每次只问必要问题；给 2-3 个方案 + 推荐，但等用户确认影响实现的选择。
5. **更新文档**：新需求 → 更新 spec/delta；技术方案变化 → 更新 design.md；用户可见变化 → 更新 README。

---

## 清晰度检查清单

进入计划或开发前必须能回答：

- 要解决什么问题？不做什么？
- 涉及哪些 spec/docs？
- 验收标准是什么？
- 测试如何证明完成？
- 哪些文档需同步？

任一无法回答 → 停止，先澄清。

---

## 禁止事项

- 禁止用「合理默认值」替代用户确认。
- 禁止把 AI 建议写成已确认需求。
- 禁止在缺少验收标准时开始实现。
- 禁止只更新代码不更新受影响文档。
- 禁止因想保持推进速度而跳过澄清。
