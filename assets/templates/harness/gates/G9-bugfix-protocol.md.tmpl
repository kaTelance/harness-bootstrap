# BugFix 验证协议（G9）

> 约束 AI 在每次 bug fix 时的行为。强制根因分析 + 候选方案 + 冷却期验证。

---

## 核心原则

bug fix 无例外。每次 fix 必须先定位根因（非治标），列 ≥ 2 候选方案，修复后经过冷却期（回归 + 反证自检 + 置信度评估）才能 commit。

---

## 触发条件

- 每次 bug fix（无例外）

---

## 必做流程

1. **复现**：稳定复现 bug（最小复现路径）。
2. **根因分析**：追踪完整链路定位根因（非表面症状）。产出根因分析。
3. **候选方案**：脑暴 ≥ 2 个修复方案，评估各自影响 / 风险 / 彻底程度。
4. **选定方案**：选最彻底且风险可控的方案。
5. **修复**：实现 + 测试（先写复现测试，验证修复有效）。
6. **冷却期**（修复后才可 commit）：
   - 回归测试：跑相关测试 + 全量回归。
   - 反证自检：「这个修复是否真的解决了根因？是否有其他场景仍会触发？」
   - 置信度评估：高 / 中 / 低，低置信度需进一步验证。

---

## 检查清单

- [ ] 能稳定复现吗？
- [ ] 根因定位了吗（非治标）？
- [ ] 有 ≥ 2 候选方案吗？
- [ ] 修复有测试覆盖吗（先写复现测试）？
- [ ] 冷却期三项（回归 / 反证 / 置信度）都做了吗？

---

## 禁止事项

- 禁止不定位根因就「打补丁」。
- 禁止不写复现测试就修复。
- 禁止跳过冷却期直接 commit。
- 禁止低置信度就声明修复完成。
