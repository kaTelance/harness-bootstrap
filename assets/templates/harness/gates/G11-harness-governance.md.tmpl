# Harness 治理变更闸门（G11）

> 约束 AI 在修改 `.harness/` 治理文件或入口文件（CLAUDE.md / AGENTS.md）时的行为。防止破坏 harness 一致性。

---

## 核心原则

`.harness/` 治理文件（规则 / 闸门 / hooks / L0 宪法）和入口文件的修改是高风险变更，必须明确意图、评估影响、保持一致性。

---

## 触发条件

- 修改 `.harness/` 下任何文件
- 修改 `CLAUDE.md` / `AGENTS.md` 入口文件
- 新增 / 删除闸门、规则、hook

---

## 必做流程

1. 明确变更意图（为什么改治理文件）。
2. 评估影响：改动是否影响其他闸门 / 规则的引用？
3. 保持引用一致：闸门编号、文件路径、交叉引用同步更新。
4. 保持分层一致：L0 宪法 / L1 法典 / L2 闸门不混淆。
5. 入口文件改动 → 验证导入链（`@file` / `@path` 导入）不断裂。
6. hook 改动 → 验证 `.claude/settings.json` 引用一致。

---

## 检查清单

- [ ] 变更意图明确吗？
- [ ] 影响评估做了吗（哪些引用受影响）？
- [ ] 交叉引用同步了吗（闸门编号 / 路径）？
- [ ] 分层没有混淆吗（L0 / L1 / L2）？
- [ ] 导入链 / settings 引用一致吗？

---

## 禁止事项

- 禁止无意图随意改治理文件。
- 禁止改闸门不同步编号 / 引用。
- 禁止破坏 L0 / L1 / L2 分层。
- 禁止改 hook 不同步 settings.json。
