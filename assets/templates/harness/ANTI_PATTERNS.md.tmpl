# 反模式库

> 闸门执行前 MUST 先读本文件对照已知反模式。hook 反馈反复触发同一问题时，在此沉淀新条目。

---

## AP-01: 「改动很小」跳过闸门

**症状**：AI 认为「只改一行 / 只是小修」而跳过 G4 / G5 评估。

**危害**：小改动往往是公共接口 / 热路径，跳过评估导致回归。

**正确做法**：闸门无大小之分。任何已有代码修改都过 G4 / G5。「改动很小」不构成豁免理由。

---

## AP-02: 治标式 bug fix

**症状**：bug fix 只消除表面症状，不定位根因。

**危害**：同类 bug 反复出现；修复可能引入新问题。

**正确做法**：走 G9 协议。先定位根因（根因分析），再修复。禁止「打补丁」式修复。

---

## AP-03: 绕过 hook

**症状**：hook 提醒 / 阻断后，AI 用 Bash 绕过 Write / Edit 闸门（如 `cat > file`、`sed -i`）来规避审查。

**危害**：破坏 harness 纪律；规避代码影响审查；隐藏真实变更。

**正确做法**：正面响应 hook 反馈（G12）。理解反馈 → 修正 → 验证闭环。禁止用其他工具绕过。

---

## 沉淀新反模式

<!-- 当 hook 反馈反复触发同一问题、或同一类错误反复出现时，在此追加 AP-NN 条目。
     格式：症状 / 危害 / 正确做法。 -->

