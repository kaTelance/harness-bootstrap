# 代码影响深度审查

> 对应闸门 G5。详细定义「三次阅读 + 行为依赖表」流程。即 Read Before Write 铁律。

---

## 三次阅读

修改任何已有函数前，必须完成以下三次阅读。

### 第一读：目标函数

读要修改的函数。理解：

- 签名（参数 / 返回 / 泛型 / 异常）
- 当前行为（做什么）
- 边界条件（空 / null / 越界 / 并发）
- 副作用（IO / 状态变更 / 事件触发）
- 错误处理（抛异常 / 返回错误码 / 静默吞掉）

### 第二读：调用方

grep（或 CodeGraph）所有调用点。对每个调用方理解：

- 传入什么输入（调用方假设的输入约束）
- 依赖什么输出（期望的返回值 / 副作用）
- 如何处理异常（是否依赖异常类型）

### 第三读：被调用函数

读目标函数依赖的下游函数，理解它们的契约（参数 / 返回 / 异常 / 副作用）。

---

## 行为依赖表

产出以下表格作为修改决策依据：

| 维度     | 内容                                         |
| -------- | -------------------------------------------- |
| 当前行为 | 函数做什么、契约是什么                       |
| 调用方   | 谁依赖它、各自依赖什么行为                   |
| 被调用   | 它依赖什么下游函数                           |
| 修改影响 | 改后哪些调用方的预期会受影响                 |
| 风险点   | 哪些调用方可能 break、哪些边界条件会变化     |

---

## 修改决策

基于行为依赖表判断：

- 是否破坏契约？→ 评估所有调用方
- 是否有更安全的改法？→ 优先非破坏性方案
- 是否需要迁移？→ 提供兼容路径 + 兼容期

---

## 禁止事项

- 禁止不读完三次就改。
- 禁止假设「调用方不会受影响」而不去 grep 验证。
- 禁止修改公共签名而不产出行为依赖表。
