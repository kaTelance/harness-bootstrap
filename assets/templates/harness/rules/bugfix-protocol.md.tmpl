# BugFix 验证协议

> 对应闸门 G9 / G10。详细定义 bug fix 的完整流程、根因分析、候选方案、冷却期、阶段升级。

---

## 阶段一：复现与根因

### 1.1 稳定复现

- 找到最小复现路径
- 记录复现条件（环境 / 数据 / 操作序列）
- 写成可重复执行的测试（先验证 bug 存在 = 测试失败）

### 1.2 根因分析（非治标）

追踪完整链路，定位根因而非表面症状。产出：

- 表面症状：观察到的问题
- 根因：导致症状的根本原因
- 触发条件：什么情况下根因导致症状

---

## 阶段二：方案设计

### 2.1 候选方案（≥ 2）

脑暴至少 2 个修复方案，对比评估：

| 方案 | 彻底程度 | 影响范围 | 风险 | 实现成本 |
| ---- | -------- | -------- | ---- | -------- |

### 2.2 选定

选最彻底且风险可控的方案，说明为什么不选其他方案。

---

## 阶段三：修复

1. **先写复现测试**：验证 bug 存在（测试失败）。
2. **实现修复**：使复现测试通过。
3. **扩展测试**：覆盖边界条件 / 其他可能触发场景。

---

## 阶段四：冷却期（修复后才可 commit）

### 4.1 回归测试

跑相关测试 + 全量回归。

### 4.2 反证自检

- 这个修复真的解决了根因吗？
- 是否有其他场景仍会触发？
- 修复是否引入了新问题？

### 4.3 置信度评估

- 高：根因明确，修复彻底，测试充分
- 中：根因较明确，修复有效，但有未覆盖场景
- 低：根因不确定，修复可能是治标

**低置信度 → 不可 commit**，继续排查或升级到阶段五。

---

## 阶段五：重复修复升级（G10）

同一 bug 修复失败 2 次后：

1. 停止相同思路重试
2. 复盘前 2 次的共同假设
3. 质疑前提（根因假设是否错了？复现路径是否完整？）
4. 扩大假设空间（并发 / 时序 / 环境 / 数据相关 / 第三方行为）
5. 引入外部视角（领域专家子代理 / 用户审查）
6. 竞争性修复（并行 ≥ 2 个 debugger 子代理走不同方案，若工具支持）
