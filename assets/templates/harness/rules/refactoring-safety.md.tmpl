# 重构安全

> 对应闸门 G8。详细定义状态迁移 / 事件链路 / 盒模型重构的等价性验证。

---

## 前置：测试覆盖

重构前确认有足够测试证明当前行为。不足则先补 characterization tests（锁定现有行为，即使现有行为有缺陷也先锁定，重构后再决定是否修复）。

---

## 状态迁移安全

### 状态形状变化

- 新增字段：默认值兼容旧数据
- 删除 / 重命名字段：提供迁移逻辑
- 类型变化：提供转换 + 兼容期

### 迁移策略（逐步）

1. **双写**：新旧状态同时写
2. **迁移**：存量数据转换
3. **读兼容**：读时兼容新旧格式
4. **清理**：确认无旧数据后移除兼容代码

---

## 事件链路安全

- 触发顺序：验证不变
- 监听者：无遗漏 / 无重复
- 阻断 / 冒泡：行为一致

验证方法：列出所有监听者，逐一确认重构前后行为一致。

---

## 盒模型 / 布局安全

- 视觉回归对比（关键视图前后一致）
- 响应式断点验证
- 布局依赖（flex / grid 改动是否影响相邻元素）

---

## 小步重构原则

- 每步可独立验证（每步一个 commit）
- 行为不变原则（重构不改变行为，除非走 bugfix 协议）
- 出错可快速回退
