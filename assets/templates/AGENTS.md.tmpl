# {{PROJECT_NAME}} - 通用 AI 配置

> 所有 AI 编程工具的入口。核心规则引用 `.harness/L0/`，与 CLAUDE.md 共享同一治理体系。

本文件供非 Claude Code 的 AI 工具（或通用标准）加载。Claude Code 优先读 `CLAUDE.md`。

## 行为规则与项目上下文

见 `.harness/L0/ai-behavior.md` 和 `.harness/L0/project-context.md`。

## 闸门纪律

开发前遵循 `.harness/gates/index.md` 定义的闸门顺序。详见 `.harness/progress.md` 当前搭建状态。
